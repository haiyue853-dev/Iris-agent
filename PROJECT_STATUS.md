# Iris Agent 当前项目状态

更新时间：2026-08-13  
当前分支：`main`  
最新提交：`bb2b8fc fix: 通知写入失败不影响扫描结果`

## 已完成

### 主对话 Agent

- 支持 OpenAI / DeepSeek 兼容模型配置、NDJSON 流式对话、会话持久化、新建、切换、删除与重试。
- 内置只读工具：当前时间、工作区目录列表、UTF-8 文本读取。
- 支持 MCP 工具调用：只读工具直接执行；非只读工具须在聊天界面确认后执行。

### AI 日报与流程图

- AI 日报支持编辑、AI 修订、版本恢复、附件提取、日报内聊天和 Markdown 下载。
- 支持 TXT、Markdown、DOCX、PDF、XLSX、XLS 附件提取及容量限制。
- UML 页面使用 Mermaid 生成并导入 Draw.io，支持持续编辑与 PNG/SVG/XML 导出。

### 热点雷达与自动化

- 支持关键词订阅、扫描任务、定时调度、去重、执行记录和失败来源追踪。
- 新热点会生成站内通知；通知可标记已读或删除，并关联到热点条目。
- 自动化页面展示未读通知、扫描新增数量、失败来源及最近执行详情。
- 通知持久化异常不会影响已完成的热点扫描结果。

### MCP 连接中心

- 可管理本地 stdio MCP 服务：创建、启停、删除、工具发现、白名单、环境变量键名与响应超时。
- 已发现且在白名单内的工具会注册到主对话 `ToolRegistry`，命名为 `mcp__<server_id>__<tool_name>`。
- MCP 服务使用持久 stdio 会话；停用、改环境变量、改超时或异常时会安全关闭并在下次调用重连。
- 页面展示已启用服务、授权工具、只读自动执行工具及最近连接/调用活动。
- 安全事件只保存服务 ID、事件类别、工具名、结果、耗时和时间；绝不保存工具参数、返回值、环境变量值或原始错误内容。

## 当前结构

```text
iris_agent/
├─ api/             FastAPI 路由与请求模型
├─ core/            Agent 循环、消息与确认流程
├─ mcp_center/      MCP 配置、运行时会话、工具适配与安全事件
├─ hot_radar/       热点订阅与扫描
├─ automation/      例行扫描与执行记录
├─ notifications/   站内通知存储
├─ reports/         AI 日报、附件、版本与聊天
├─ skill_center/    内置 Skill 目录与状态
├─ tools/           内置工具与 ToolRegistry
└─ web-react/src/   React 前端、API 客户端与主题样式
```

## 已知限制

1. 尚未用真实 Browser MCP 做一次完整的“主对话发起调用 → 页面展示结果”的人工端到端验收。
2. MCP 最近活动只保留最新 50 条，适合诊断，不作为长期审计日志。
3. 前端构建仍有 Mermaid 相关的大 chunk 警告，不影响构建或运行。
4. 图片 OCR 需要额外配置 OCR 实现；未配置时图片附件会标为不可提取。

## 下一步建议

1. 配置并验收 Browser MCP 的真实只读工具，完成端到端调用验证。
2. 根据真实调用结果补充 MCP 页面的人机提示，例如连接失败原因的安全摘要和重新检测入口。
3. 清理已废弃的 React Flow 代码与依赖，降低前端包体积和维护成本。

## 最近验证记录

- 后端全量测试：`221 passed, 3 skipped`。
- MCP 工具调用、审批、持久会话、事件记录与 API 均有自动化测试覆盖。
- 前端 MCP 页面测试和生产构建此前已通过；构建仅包含大 chunk 警告。
